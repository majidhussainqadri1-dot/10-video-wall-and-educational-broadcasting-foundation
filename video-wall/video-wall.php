<?php
/**
 * Historical File 10 source archive marker.
 *
 * This directory is retained only as repository history from the pre-canonical
 * `video-wall` implementation. It is intentionally NOT a WordPress plugin
 * entrypoint and must never register activation hooks, runtime hooks, routes,
 * writes, or a second source of truth.
 *
 * Canonical runtime: ../video-wall-and-live-broadcasting/
 */

defined( 'ABSPATH' ) || exit;
