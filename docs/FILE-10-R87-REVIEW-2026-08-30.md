# File 10 — R87 Review — External-effect idempotency and compensation

## Review discipline
R87 was completed as a full uninterrupted review before any correction decision. No source correction was started during the review.

## Reviewed surface
- REST mutation idempotency and command replay state in `VWLB_Security`.
- R60 durable external-effect guard, shutdown handling, uncertain-outcome classification and diagnostics resolution path.
- R64 video-intelligence external-effect guard.
- Live scheduling/provider-create compensation and command idempotency.
- R46 stream-credential provider compensation.
- Future live-policy application, simulcast reservation/finalization and media-track generation cancellation/reconciliation.

## Frozen finding
**Clean round — no new unresolved defect was proven.**

The apparent risk in `future-config/apply` is already covered by R60: that route is explicitly an external-effect route; a required idempotency key is bound to a durable guard before the provider call; HTTP 5xx on that route is classified as uncertain; and the guard is retained as `reconcile_required` rather than released. The same framework covers live scheduling, credentials, emergency-end, simulcast and track-generation external effects. R64 separately protects video-intelligence suggestions. Simulcast also reserves local state before the provider action, while track-generation has explicit cancellation/reconciliation if local candidate persistence fails.

No correction is required for R87. The complete File 10 release QA must be green before R88 begins.
