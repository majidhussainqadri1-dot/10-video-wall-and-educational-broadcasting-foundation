name: Legacy Baseline Integrity (Archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archival-note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain historical boundary
        run: |
          echo 'Historical video-wall 0.1.0/0.2.0 package checks are archived and are not the release gate for the canonical File 10 runtime.'
          echo 'Use File 10 Release QA for video-wall-and-live-broadcasting source/package evidence.'
