name: Legacy Corrected Release QA (Archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archival-note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical release gate
        run: |
          echo 'This workflow is retained only as historical File 10 provenance for the retired video-wall 0.2.0 package.'
          echo 'The canonical video-wall-and-live-broadcasting source is validated by .github/workflows/file10-release.yml.'
